import { FunctionComponent } from "react";
import styles from "./WebDesigner.module.css";

const WebDesigner: FunctionComponent = () => {
  return <span className={styles.webDesigner}>web designer</span>;
};

export default WebDesigner;

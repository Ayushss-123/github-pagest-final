import { FunctionComponent } from "react";
import styles from "./FrontEndDeveloper.module.css";

const FrontEndDeveloper: FunctionComponent = () => {
  return <span className={styles.frontEndDeveloper}>front-end developer</span>;
};

export default FrontEndDeveloper;
